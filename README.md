# Dexplorer
A file explorer made in python  
It can open files and folders, see mini-previews of pictures without opening them and it's optimized to open large folders
## How to modify the code
Install [python](https://www.python.org) and install the requirements with  
`pip install -r requirements.txt`
